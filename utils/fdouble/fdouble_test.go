package fdouble

import (
	"testing"
)

// type FSStub struct {
// }

// func (fs *FSStub) GetAllFiles(dir string) ([]fs.FileInfo, error) {

// }

func TestScanDir(t *testing.T) {
	// ll := logrus.New()
	// wg := &sync.WaitGroup{}
	// files := make(chan FDescr)

	// fs := &FSStub{}

	// ScanDir("abc", wg, files, fs, ll)
}
