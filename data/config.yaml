# Output of detailed debugging information (default: false)
# debug: false

# Run in batch/no interactive mode (default: false)
# batch: false

# The path to the file *.scv with data (recuired)
data_file: "./data/test1.csv"

# Timeout to filtering data, ms (default: 1sec)
# filter_timeout: 1000

# Log file for user input (default: ./access.log)
# log_access: "./access.log"

# Log file for errors (default: ./errors.log)
# log_errors: "./errors.log"
